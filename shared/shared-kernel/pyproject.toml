[project]
name = "shared-kernel"
version = "0.1.0"
description = "Bloques de construcción DDD compartidos (Money, Entity, DomainEvent)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_kernel"]
