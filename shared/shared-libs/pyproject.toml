[project]
name = "shared-libs"
version = "0.1.0"
description = "Utilidades técnicas transversales (logging, configuración, EventBus)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_libs"]
