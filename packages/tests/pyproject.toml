[project]
name = "tests"
version = "0.1.0"
description = "Suite de tests del taxímetro (pytest + Allure)"
requires-python = ">=3.12"
dependencies = [
    "cli",
    "ride",
    "pricing",
    "shared-kernel",
    "shared-libs",
]

[tool.uv.sources]
cli = { workspace = true }
ride = { workspace = true }
pricing = { workspace = true }
shared-kernel = { workspace = true }
shared-libs = { workspace = true }

[tool.uv]
package = false
