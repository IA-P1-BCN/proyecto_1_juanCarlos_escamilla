[project]
name = "pricing"
version = "0.1.0"
description = "Contexto pricing — motor de tarifas y cálculo del Importe"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pricing"]
